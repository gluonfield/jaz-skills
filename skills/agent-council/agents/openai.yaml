interface:
  display_name: "Agent Council"
  short_description: "Run multiple agents, compare, and synthesize"
  default_prompt: "Investigate this decision with an independent agent council and synthesize the result."

policy:
  allow_implicit_invocation: true
