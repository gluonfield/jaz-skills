interface:
  display_name: "Agent Council"
  short_description: "Run multiple agents, compare, and synthesize"
  default_prompt: "Use $agent-council to investigate this decision with independent agents and synthesize the result."

policy:
  allow_implicit_invocation: true
