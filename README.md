# Jaz Skills

First-party Jaz skills distributed outside the desktop/backend binary.

Jaz downloads `manifest.json` from the latest GitHub release during backend
startup and installs verified skill archives into `~/.jaz/skills` in the
background. Startup and offline use continue through Jaz's embedded core skill
fallbacks.

